export const LOGIN_SAGAS = "LOGIN_SAGAS";

export const loginSagas = (data) =>({
    type: LOGIN_SAGAS,
    data
})

export const REGISTER_SAGAS = "REGISTER_SAGAS";

export const registerSagas = (data) =>({
    type: REGISTER_SAGAS,
    data
})
